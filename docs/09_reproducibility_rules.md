# Reproducibility Rules

Every run must store:
- git commit
- command line
- config file
- seed
- dataset name and split
- corpus version
- embedding model
- retriever settings
- reranker settings
- generator settings
- output artifacts
- metric JSON

Every metric table must link to run ids.
Every chart must link to source metrics.
Every result must have a reproduction command.
