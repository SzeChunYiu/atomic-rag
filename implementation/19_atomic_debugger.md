# Atomic Debugger

Build tools that answer:
- why was this chunk retrieved?
- why was this atom detected?
- why was this atom selected?
- why was this atom dropped?
- which query facet does it cover?
- what score component dominated?
- what would change if threshold moved?

Output one inspectable trace per query.
This is central to atomic understanding.
