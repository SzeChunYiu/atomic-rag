# Benchmark Runner

Responsibilities:
- run configured systems
- enforce same dataset split
- enforce same candidate budget
- collect metrics
- write comparison reports

Runner should support:
- one dataset, one system
- one dataset, many systems
- many datasets, many systems
- ablation grids

Every run gets a unique run id.
